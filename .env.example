# =============================================================================
# ACTIVE DIRECTORY CONFIGURATION
# =============================================================================
# This file shows all the AD environment variables needed for the application
# to connect to your Active Directory instance. Copy this to .env and update
# the values for your environment.

# AD Server Address (IP or hostname)
# Example: 192.168.1.100, ad.company.local, etc.
AD_SERVER=192.168.1.100

# AD LDAP Port (389 for standard LDAP, 636 for LDAPS/SSL)
AD_PORT=389

# Use SSL/LDAPS for connection (True or False)
AD_USE_SSL=False

# Base Distinguished Name (starting point for all searches)
# Format: DC=xxx,DC=xxx
# Example for eissa.local: DC=eissa,DC=local
AD_BASE_DN=DC=eissa,DC=local

# Domain Name (used for UPN format user@domain)
# Example: eissa.local, company.com, etc.
AD_DOMAIN=eissa.local

# =============================================================================
# DJANGO SETTINGS
# =============================================================================
DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

# =============================================================================
# Database Configuration (optional - defaults to Django default)
# =============================================================================
# DATABASE_URL=mssql://user:password@localhost/dbname

# =============================================================================
# Usage Instructions:
# =============================================================================
# 1. Configuration is read from environment variables with fallback defaults
# 2. The application is designed to work with ANY Active Directory server
# 3. Simply update the AD_* variables to match your AD environment
# 4. The app will automatically sync users from the configured AD_BASE_DN
#
# Example for different AD domains:
# - For Microsoft Active Directory: DC=company,DC=com
# - For OpenLDAP: ou=users,dc=company,dc=local
# - For custom domains: DC=your,DC=domain,DC=local
#
# OUs Configuration:
# - The app expects OUs to be configured in core/ldap_utils.py (AVAILABLE_OUS)
# - For production eissa.local: OUs are under OU=New,DC=eissa,DC=local
# - Available departments: Accountant, HR, IT, Projects, Sales, etc.
# - Each OU has both English and Arabic names
#
# Sync Command:
# - python manage.py sync_ad_users --ou "OU=New" --update
# - This syncs all users from the specified OU and updates existing ones
#
# Connection Flow:
# 1. Application reads AD_* settings from environment
# 2. Connects to AD_SERVER:AD_PORT using LDAP (or LDAPS if AD_USE_SSL=True)
# 3. Uses AD_BASE_DN as search base
# 4. Admin account uses administrator@AD_DOMAIN format for binding
